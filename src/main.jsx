import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

//architecture
//bootstrap
//component
//rules --1 parent + classname +fn first letter Capital
//exports --named export + default
//react ---history + faster (JSX)
//vite cra dep
